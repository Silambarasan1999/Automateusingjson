export const handler = async (event) => {
  // TODO implement
  const response = {
    statusCode: 200,
    body: JSON.stringify("Hello silambu demo I'm lambda from repo using json config file"),
  };
  return response;
};
